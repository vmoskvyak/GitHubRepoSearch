<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.vmoskvyak.githubreposearch.ui.fragments.main.SearchGitRepoItemViewModel" />
        <variable
            name="click"
            type="com.vmoskvyak.githubreposearch.ui.adapters.GitRepoAdapter.OnGitRepoInfoClickListener"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:background="@android:color/white"
        android:orientation="horizontal"
        android:onClick="@{()->click.onGitRepoInfoClick(viewModel)}">

        <ImageView
            android:id="@+id/iv_avatafr"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:imageUrl="@{viewModel.getAvatarUrl()}"
            tools:ignore="ContentDescription"
            tools:src="@tools:sample/avatars" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="18dp"
            android:layout_marginTop="1dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/tv_name"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:lines="1"
                    android:text="@{viewModel.getName()}"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    tools:text="test" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:drawableStart="@drawable/ic_code_fork_symbol"
                    android:drawablePadding="5dp"
                    android:text="@{viewModel.getNumberOfForks()}"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    tools:text="20" />
            </LinearLayout>

            <TextView
                android:id="@+id/tv_description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:ellipsize="end"
                android:text="@{viewModel.getDescription()}"
                android:textSize="16sp"
                tools:text="test" />
        </LinearLayout>
    </LinearLayout>
</layout>